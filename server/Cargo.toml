[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = "1.0"
sudoku-lib = { path = "../lib" }
sp1-sdk = "5.0.0"
dotenv = "0.15.0"

[build-dependencies]
sp1-build = "5.0.0"
